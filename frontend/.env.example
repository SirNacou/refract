# Application Configuration
VITE_APP_TITLE="Refract - URL Shortener"

# API Configuration
VITE_API_BASE_URL=http://localhost:8080

# OIDC Configuration (Generic OpenID Connect)
# Works with any OIDC provider: Zitadel, Keycloak, Auth0, Okta, etc.

# OIDC Issuer URL (the base URL of your identity provider)
# Examples:
#   Zitadel: https://your-instance.zitadel.cloud
#   Keycloak: https://keycloak.example.com/realms/your-realm
#   Auth0: https://your-tenant.auth0.com
VITE_OIDC_ISSUER=https://your-oidc-provider.example.com

# Client ID from your OIDC application configuration
VITE_OIDC_CLIENT_ID=your-client-id-here

# Redirect URI after successful login (must match OIDC app config)
VITE_OIDC_REDIRECT_URI=http://localhost:3000/auth/callback

# Optional: Where to redirect after logout
VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# Optional: OIDC scopes (defaults to "openid profile email" if not set)
# Add "offline_access" if you need refresh tokens
VITE_OIDC_SCOPES=openid profile email
